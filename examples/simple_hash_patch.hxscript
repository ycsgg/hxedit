let needle = hx_hex("de ad be ef");
let hit = hx_search_forward_select(needle);

if hit < 0 {
    throw "needle not found";
}

let digest = hx_hash_selection_hex("crc32");
hx_overwrite(hit + 4, hx_ascii(digest));
hx_save();
